<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to 'Neo Table Links', root_path, class:"navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "About Us", table_links_about_path, class:"nav-link"%>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link" %>
          </li>

          <li class="nav-item">
            <%= link_to 'Sign Out', destroy_user_session_path, class: 'nav-link text-danger', data: { turbo_method: :delete } %>
          </li>
          <% if current_user.role == 'Admin' %>
            <li class="nav-item">
              <%# Link to View All Users %>
            </li>
            <li class="nav-item">
              <%# Link to View All Restaurants %>
            </li>
            <li class="nav-item">
              <%# Link to View All Reservations %>
            </li>
            <% elsif current_user.role == 'Owner' %>
              <li class="nav-item">
                <%= link_to "Create A Restaurant", new_restaurant_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%# Link to View All Restaurant that they have created %>
              </li>
              <li class="nav-item">
                <%# Link to View All Reservations Associated with their Restaurants %>
              </li>
            <% elsif current_user.role == 'Customer' %>
              <li class="nav-item">
                <%# Link to Create a Reservation %>
              </li>
              <li class="nav-item">
                <%# Link to View All fo your Reservations %>
              </li>
            <% else %>
              <li class="nav-item">
                <p>Seems you are an Unkown User</p>
              </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

